version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: cfct_db
      POSTGRES_USER: cfct_user
      POSTGRES_PASSWORD: SecurePass123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
